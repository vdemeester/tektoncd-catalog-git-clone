# This file is generated from task/git-clone/git-clone.yaml
# Do not edit directly — run: ./hack/generate-stepaction.sh
apiVersion: tekton.dev/v1beta1
kind: StepAction
metadata:
  name: git-clone
  labels:
    app.kubernetes.io/version: "1.7.1"
  annotations:
    artifacthub.io/category: integration-delivery
    artifacthub.io/changes: |
      - kind: fixed
        description: "Prevent credential leakage from URLs in logs, results, and shell traces"
      - kind: fixed
        description: "Prevent git option injection via revision/refspec in fetch, checkout, and show commit"
      - kind: fixed
        description: "Validate subdirectory and userHome params to prevent path traversal and credential copy to shared workspace; clean up credentials on step exit"
      - kind: changed
        description: "Bump alpine/git base image to latest version"
      - kind: changed
        description: "Bump GitHub Actions dependencies (checkout, setup-go, setup-ko, setup-crane, goreleaser-action, setup-buildx-action, setup-qemu-action)"
      - kind: changed
        description: "Group dependabot updates and fix gomod path"
      - kind: changed
        description: "Drop Trusted Resources signature from release"
      - kind: added
        description: "Add Artifact Hub StepActions badge and browse links"
      - kind: added
        description: "Add DEVELOPMENT.md, CONTRIBUTING.md, and AGENTS.md documentation"
      - kind: changed
        description: "Make e2e tests more robust against flakes"
    artifacthub.io/license: Apache-2.0
    artifacthub.io/links: |
      - name: support
        url: https://github.com/tektoncd-catalog/git-clone/issues
    artifacthub.io/maintainers: |
      - name: Vincent Demeester
        email: vincent@sbr.pm
      - name: vinamra28
        email: jvinamra776@gmail.com
    artifacthub.io/provider: Tekton
    tekton.dev/catalog: tektoncd.git-clone
    tekton.dev/catalog-support-tier: verified
    tekton.dev/catalog-url: https://github.com/tektoncd-catalog/git-clone
    tekton.dev/categories: Git
    tekton.dev/displayName: git clone
    tekton.dev/pipelines.minVersion: "1.0.0"
    tekton.dev/platforms: linux/amd64,linux/s390x,linux/ppc64le,linux/arm64
    tekton.dev/tags: git
spec:
  description: |-
    This StepAction clones a git repository for use by other steps in your Pipeline.
    It clones a repo from the provided url into the output path. By default the repo will be cloned into the root of the provided path. You can clone into a subdirectory by setting this StepAction's subdirectory param. This StepAction also supports sparse checkouts. To perform a sparse checkout, pass a list of comma separated directory patterns to this StepAction's sparseCheckoutDirectories param.
  params:
  - name: output-path
    description: The git repo will be cloned onto this path.
    type: string
  - name: ssh-directory-path
    description: |
      A .ssh directory with private key, known_hosts, config, etc.
    default: ""
  - name: basic-auth-path
    description: |
      A directory path containing a .gitconfig and .git-credentials file.
    default: ""
  - name: ssl-ca-directory-path
    description: |
      A directory containing CA certificates for HTTPS verification.
    default: ""
  - description: Repository URL to clone from.
    name: url
    type: string
  - default: ""
    description: Revision to checkout. (branch, tag, sha, ref, etc...)
    name: revision
    type: string
  - default: ""
    description: Refspec to fetch before checking out revision.
    name: refspec
  - default: "true"
    description: Initialize and fetch git submodules.
    name: submodules
    type: string
  - default: ""
    description: |
      Comma-separated list of specific submodule paths to initialize and fetch. Only submodules in the specified directories and their subdirectories will be fetched.
      Empty string fetches all submodules. Parameter "submodules" must be set to "true" to make this parameter applicable.
    name: submodulePaths
    type: string
  - default: "1"
    description: Perform a shallow clone, fetching only the most recent N commits.
    name: depth
    type: string
  - default: "true"
    description: Set the `http.sslVerify` global git config. Setting this to `false`
      is not advised unless you are sure that you trust your git remote.
    name: sslVerify
    type: string
  - default: ca-bundle.crt
    description: file name of mounted crt using ssl-ca-directory workspace. default
      value is ca-bundle.crt.
    name: crtFileName
    type: string
  - default: ""
    description: Subdirectory inside the `output` Workspace to clone the repo into.
    name: subdirectory
    type: string
  - default: ""
    description: Define the directory patterns to match or exclude when performing
      a sparse checkout.
    name: sparseCheckoutDirectories
    type: string
  - default: "true"
    description: Clean out the contents of the destination directory if it already
      exists before cloning.
    name: deleteExisting
    type: string
  - default: ""
    description: HTTP proxy server for non-SSL requests.
    name: httpProxy
    type: string
  - default: ""
    description: HTTPS proxy server for SSL requests.
    name: httpsProxy
    type: string
  - default: ""
    description: Opt out of proxying HTTP/HTTPS requests.
    name: noProxy
    type: string
  - default: "true"
    description: Log the commands that are executed during `git-clone`'s operation.
    name: verbose
    type: string
  - default: "true"
    description: |
      Print user-friendly error messages with actionable hints and reproduction commands when git operations fail. Set to "false" to disable.
    name: userFriendlyErrors
    type: string
  - default: ghcr.io/tektoncd-catalog/git-clone:v1.7.1
    description: The image providing the git-init binary that this StepAction runs.
    name: gitInitImage
    type: string
  - default: /tekton/home
    description: |
      Absolute path to the user's home directory.
    name: userHome
    type: string
  results:
  - description: The precise commit SHA that was fetched by this StepAction.
    name: commit
  - description: The precise URL that was fetched by this StepAction.
    name: url
  - description: The epoch timestamp of the commit that was fetched by this StepAction.
    name: committer-date
  image: $(params.gitInitImage)
  env:
  - name: HOME
    value: $(params.userHome)
  - name: PARAM_URL
    value: $(params.url)
  - name: PARAM_REVISION
    value: $(params.revision)
  - name: PARAM_REFSPEC
    value: $(params.refspec)
  - name: PARAM_SUBMODULES
    value: $(params.submodules)
  - name: PARAM_SUBMODULE_PATHS
    value: $(params.submodulePaths)
  - name: PARAM_DEPTH
    value: $(params.depth)
  - name: PARAM_SSL_VERIFY
    value: $(params.sslVerify)
  - name: PARAM_CRT_FILENAME
    value: $(params.crtFileName)
  - name: PARAM_SUBDIRECTORY
    value: $(params.subdirectory)
  - name: PARAM_DELETE_EXISTING
    value: $(params.deleteExisting)
  - name: PARAM_HTTP_PROXY
    value: $(params.httpProxy)
  - name: PARAM_HTTPS_PROXY
    value: $(params.httpsProxy)
  - name: PARAM_NO_PROXY
    value: $(params.noProxy)
  - name: PARAM_VERBOSE
    value: $(params.verbose)
  - name: PARAM_USER_FRIENDLY_ERRORS
    value: $(params.userFriendlyErrors)
  - name: PARAM_SPARSE_CHECKOUT_DIRECTORIES
    value: $(params.sparseCheckoutDirectories)
  - name: PARAM_USER_HOME
    value: $(params.userHome)
  - name: PARAM_OUTPUT_PATH
    value: $(params.output-path)
  - name: PARAM_SSH_DIRECTORY_PATH
    value: $(params.ssh-directory-path)
  - name: PARAM_BASIC_AUTH_DIRECTORY_PATH
    value: $(params.basic-auth-path)
  - name: PARAM_SSL_CA_DIRECTORY_PATH
    value: $(params.ssl-ca-directory-path)
  securityContext:
    runAsNonRoot: true
    runAsUser: 65532
  script: |
    #!/bin/sh
    set -eu

    if [ "${PARAM_VERBOSE}" = "true" ] ; then
      set -x
    fi

    case "${PARAM_USER_HOME}" in
      "${PARAM_OUTPUT_PATH}"|"${PARAM_OUTPUT_PATH}"/*)
        printf "userHome must not be inside the output workspace, got: %s\n" "${PARAM_USER_HOME}" >&2
        exit 1
        ;;
    esac

    # Ensure the user home directory exists (it may not in minimal images)
    mkdir -p "${PARAM_USER_HOME}"

    cleanup_credentials() {
      rm -f "${PARAM_USER_HOME}/.git-credentials" "${PARAM_USER_HOME}/.gitconfig"
      rm -rf "${PARAM_USER_HOME}/.ssh"
    }
    trap cleanup_credentials EXIT

    if [ "${PARAM_BASIC_AUTH_DIRECTORY_PATH}" != "" ] ; then
      cp "${PARAM_BASIC_AUTH_DIRECTORY_PATH}/.git-credentials" "${PARAM_USER_HOME}/.git-credentials"
      cp "${PARAM_BASIC_AUTH_DIRECTORY_PATH}/.gitconfig" "${PARAM_USER_HOME}/.gitconfig"
      chmod 400 "${PARAM_USER_HOME}/.git-credentials"
      chmod 400 "${PARAM_USER_HOME}/.gitconfig"
    fi

    if [ "${PARAM_SSH_DIRECTORY_PATH}" != "" ] ; then
      cp -R "${PARAM_SSH_DIRECTORY_PATH}" "${PARAM_USER_HOME}"/.ssh
      chmod 700 "${PARAM_USER_HOME}"/.ssh
      chmod -R 400 "${PARAM_USER_HOME}"/.ssh/*
    fi

    if [ "${PARAM_SSL_CA_DIRECTORY_PATH}" != "" ] ; then
       export GIT_SSL_CAPATH="${PARAM_SSL_CA_DIRECTORY_PATH}"
       if [ "${PARAM_CRT_FILENAME}" != "" ] ; then
          export GIT_SSL_CAINFO="${PARAM_SSL_CA_DIRECTORY_PATH}/${PARAM_CRT_FILENAME}"
       fi
    fi

    if [ -n "${PARAM_SUBDIRECTORY}" ] ; then
      case "${PARAM_SUBDIRECTORY}" in
        /*) printf "subdirectory param must be a relative path, got: %s\n" "${PARAM_SUBDIRECTORY}" >&2; exit 1 ;;
      esac
      if printf "%s" "${PARAM_SUBDIRECTORY}" | grep -qE '(^|/)\.\.(/|$)'; then
        printf "subdirectory param must not contain '..', got: %s\n" "${PARAM_SUBDIRECTORY}" >&2
        exit 1
      fi
    fi

    CHECKOUT_DIR="${PARAM_OUTPUT_PATH}/${PARAM_SUBDIRECTORY}"

    cleandir() {
      # Delete any existing contents of the repo directory if it exists.
      #
      # We don't just "rm -rf ${CHECKOUT_DIR}" because ${CHECKOUT_DIR} might be "/"
      # or the root of a mounted volume.
      if [ -d "${CHECKOUT_DIR}" ] ; then
        # Delete non-hidden files and directories
        rm -rf "${CHECKOUT_DIR:?}"/*
        # Delete files and directories starting with . but excluding ..
        rm -rf "${CHECKOUT_DIR}"/.[!.]*
        # Delete files and directories starting with .. plus any other character
        rm -rf "${CHECKOUT_DIR}"/..?*
      fi
    }

    if [ "${PARAM_DELETE_EXISTING}" = "true" ] ; then
      cleandir || true
    fi

    test -z "${PARAM_HTTP_PROXY}" || export HTTP_PROXY="${PARAM_HTTP_PROXY}"
    test -z "${PARAM_HTTPS_PROXY}" || export HTTPS_PROXY="${PARAM_HTTPS_PROXY}"
    test -z "${PARAM_NO_PROXY}" || export NO_PROXY="${PARAM_NO_PROXY}"

    git config --global --add safe.directory "${PARAM_OUTPUT_PATH}"

    /ko-app/git-init \
      -url="${PARAM_URL}" \
      -revision="${PARAM_REVISION}" \
      -refspec="${PARAM_REFSPEC}" \
      -path="${CHECKOUT_DIR}" \
      -sslVerify="${PARAM_SSL_VERIFY}" \
      -submodules="${PARAM_SUBMODULES}" \
      -depth="${PARAM_DEPTH}" \
      -sparseCheckoutDirectories="${PARAM_SPARSE_CHECKOUT_DIRECTORIES}" \
      -submodulePaths="${PARAM_SUBMODULE_PATHS}" \
      -userFriendlyErrors="${PARAM_USER_FRIENDLY_ERRORS}"
    cd "${CHECKOUT_DIR}"
    RESULT_SHA="$(git rev-parse HEAD)"
    EXIT_CODE="$?"
    if [ "${EXIT_CODE}" != 0 ] ; then
      exit "${EXIT_CODE}"
    fi
    RESULT_COMMITTER_DATE="$(git log -1 --pretty=%ct)"
    printf "%s" "${RESULT_COMMITTER_DATE}" > "$(step.results.committer-date.path)"
    printf "%s" "${RESULT_SHA}" > "$(step.results.commit.path)"
    { set +x; } 2>/dev/null
    REDACTED_URL="$(printf "%s" "${PARAM_URL}" | sed -E 's|://[^@/]+@|://****@|')"
    printf "%s" "${REDACTED_URL}" > "$(step.results.url.path)"
    if [ "${PARAM_VERBOSE}" = "true" ] ; then set -x; echo "results.url: ${REDACTED_URL}"; fi
